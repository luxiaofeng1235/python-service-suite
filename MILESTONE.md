# 项目里程碑

> FastAPI AI Service 当前建设进度与后续路线图。
> 更新时间：2026-05-29，按当前代码结构整理。

---

## 当前定位

这个项目已经从“AI 接口 Demo”演进成一个接近可用的 FastAPI 后端脚手架：

- 可以直接新增业务 CRUD：按 `Model + Schema + Service + Controller + Route` 的方式扩展。
- 可以直接接 AI 能力：已支持千问 OpenAI 兼容接口、普通对话、SSE 流式输出、上下文记录。
- 可以作为企业服务底座：已具备鉴权、数据库迁移、统一响应、统一异常、请求日志、文件上传、Redis 缓存等基础能力。

---

## M1：基础脚手架闭环（已完成）

| 模块 | 内容 | 状态 |
|------|------|------|
| 项目结构 | `app/main.py` 入口，`routes/api.py` 聚合路由 | 已完成 |
| 三层架构 | Controller 负责路由，Service 负责业务，Model 负责 ORM | 已完成 |
| 请求/响应模型 | Pydantic Schema 独立放在 `app/schemas` | 已完成 |
| 统一响应 | 统一返回 `{code,msg,data}`，成功码为 `1` | 已完成 |
| 全局异常 | 业务异常、参数校验异常、HTTP 异常、兜底异常 | 已完成 |
| Swagger 文档 | `/docs`、`/redoc` 自动生成接口文档与字段注释 | 已完成 |
| 配置管理 | `.env` + `app/core/config.py` 集中维护敏感配置 | 已完成 |
| 路由聚合 | 新控制器只需在 `app/routes/api.py` 注册 | 已完成 |

---

## M2：用户与权限体系（已完成基础版）

| 模块 | 内容 | 状态 |
|------|------|------|
| 用户注册 | 用户名唯一校验、密码加密、用户入库 | 已完成 |
| 用户登录 | 生成 32 位短 Token，写入 `user_tokens` | 已完成 |
| Token 鉴权 | `Authorization: Bearer <token>` 鉴权 | 已完成 |
| 免登录白名单 | 通过 `AUTH_WHITE_LIST` 统一配置 | 已完成 |
| 当前用户依赖 | `get_current_user` 注入当前用户 | 已完成 |
| 管理员依赖 | `get_current_admin` 校验 `is_super` | 已完成 |
| 退出登录 | 删除当前 Token，使登录态立即失效 | 已完成 |
| 过期 Token 清理 | 管理员接口清理过期 Token | 已完成 |
| 账号注销 | 软删除用户并清空 Token | 已完成 |
| 忘记密码 | 邮箱验证码 + Redis 缓存 + DB 兜底记录 | 已完成 |

后续增强：

| 模块 | 内容 | 优先级 |
|------|------|--------|
| RBAC 权限 | 角色表、权限表、接口权限校验 | P0 |
| 管理后台用户管理 | 禁用/启用、角色分配、用户详情 | P0 |
| Token Redis 快速校验 | 当前仍以 DB 为准，可补 Redis 加速 | P1 |
| 验证码频率限制 | 邮件验证码 60 秒冷却、每日次数限制 | P1 |

---

## M3：AI 对话能力（已完成基础版）

| 模块 | 内容 | 状态 |
|------|------|------|
| 千问接入 | OpenAI 兼容协议调用 DashScope 千问 | 已完成 |
| 模型配置 | 当前主模型为 `qwen-max`，深度思考可走 `qwen-plus` | 已完成 |
| System Prompt | 已在项目配置默认 AI 身份与回答风格 | 已完成 |
| 普通对话 | `/api/ai/chat` 返回完整回复 | 已完成 |
| SSE 流式输出 | `/api/ai/chat/send_stream_sse` 分片返回 | 已完成 |
| 旧链路兼容 | 请求体兼容 `model/chat_id/msg/restart/is_deep_reflection` | 已完成 |
| 上下文关联 | `chat_id=0` 新建，非 0 按历史上下文组装 prompt | 已完成 |
| 对话记录 | `ai_chat_log` 保存用户消息和 AI 回复 | 已完成 |
| 对话列表 | `/api/ai/chats` | 已完成 |
| 对话详情 | `/api/ai/chats/{chat_id}` | 已完成 |
| 删除对话 | `DELETE /api/ai/chats/{chat_id}` | 已完成 |
| 清空上下文 | 单独清空接口当前未落路由 | 待补 |

后续增强：

| 模块 | 内容 | 优先级 |
|------|------|--------|
| 多模型配置表 | 模型、system、搜索开关从 DB 或配置文件动态维护 | P0 |
| AI 对话用户隔离 | 登录用户按 `user_id` 查询，免登录默认 `user_id=0` | P0 |
| 清空上下文接口 | 保留对话 ID，只清空 chat 内容 | P1 |
| AI 错误重试 | 上游超时、连接失败、限流时统一重试和友好提示 | P1 |
| 用量统计 | token usage、调用次数、模型成本统计 | P1 |
| 文生图/文生视频 | 复用当前 AI 配置层接入异步任务 | P2 |

---

## M4：文件与静态资源（已完成基础版）

| 模块 | 内容 | 状态 |
|------|------|------|
| 图片上传 | `/api/file/upload/image`，无需登录 | 已完成 |
| 视频上传 | `/api/file/upload/video`，无需登录 | 已完成 |
| 文件列表 | `/api/file/list` 分页返回附件 | 已完成 |
| 附件表 | 默认表名 `attachment` | 已完成 |
| 本地静态访问 | `/uploads/**` 映射上传目录 | 已完成 |
| 配置化限制 | 格式、大小从配置读取 | 已完成 |

后续增强：

| 模块 | 内容 | 优先级 |
|------|------|--------|
| 用户文件隔离 | 登录后按 `user_id` 归属附件 | P0 |
| OSS 存储 | 本地/OSS 存储驱动切换 | P1 |
| 文件安全 | MIME 二次校验、图片压缩、违规扩展名拦截 | P1 |
| 文件删除 | 软删除附件记录并清理实体文件 | P2 |

---

## M5：数据库与缓存（已完成基础版）

| 模块 | 内容 | 状态 |
|------|------|------|
| SQLAlchemy Async | 异步 ORM 会话管理 | 已完成 |
| MySQL 支持 | `mysql+aiomysql` 连接 | 已完成 |
| Alembic 迁移 | 版本化管理表结构 | 已完成 |
| 初始化 SQL | `sql/init.sql` 保留一键初始化表结构 | 已完成 |
| Redis 集成 | 启动时初始化 Redis，失败不阻塞主服务 | 已完成 |
| 验证码缓存 | Redis 保存重置密码验证码，DB 做兜底/审计 | 已完成 |

后续增强：

| 模块 | 内容 | 优先级 |
|------|------|--------|
| 连接池参数治理 | pool size、回收时间、断线重连参数标准化 | P0 |
| Redis 限流 | 登录、验证码、AI 调用频控 | P1 |
| 缓存封装 | 统一 key 前缀、TTL、序列化策略 | P1 |

---

## M6：日志与可观测性（已完成基础版）

| 模块 | 内容 | 状态 |
|------|------|------|
| 请求日志中间件 | method、path、status、cost、client_ip | 已完成 |
| Trace ID | 响应头返回 `X-Trace-Id` | 已完成 |
| 耗时统计 | 响应头返回 `X-Process-Time` | 已完成 |
| 慢请求日志 | 超过 `SLOW_REQUEST_MS` 单独记录 | 已完成 |
| 日志文件 | 应用日志、请求日志、慢请求日志分文件 | 已完成 |
| 敏感参数脱敏 | password、token、authorization、code 等字段脱敏 | 已完成 |

后续增强：

| 模块 | 内容 | 优先级 |
|------|------|--------|
| 操作日志 | 管理员操作审计 | P0 |
| 业务日志规范 | AI 调用、文件上传、用户登录等业务事件标准化 | P1 |
| 监控指标 | Prometheus 指标、接口 QPS、错误率 | P2 |

---

## M7：部署与工程化（已完成基础版）

| 模块 | 内容 | 状态 |
|------|------|------|
| Dockerfile | 镜像构建 | 已完成 |
| docker-compose | app + mysql + redis 编排 | 已完成 |
| Makefile | dev、build、docker、alembic 常用命令 | 已完成 |
| Ruff | 代码格式化与检查配置 | 已完成 |
| `.env.example` | 环境变量模板，不包含真实密钥 | 已完成 |

后续增强：

| 模块 | 内容 | 优先级 |
|------|------|--------|
| 测试用例 | Service 单测、API 集成测试 | P0 |
| CI/CD | 自动检查、自动测试、自动部署 | P1 |
| 生产配置模板 | Nginx、systemd、Docker 生产 compose | P1 |
| 健康检查增强 | DB、Redis、上游 AI 服务健康状态 | P1 |

---

## M8：AI 业务化路线（规划中）

这部分不是“接一个千问就结束”，而是把 AI 接到公司业务数据与业务能力上。

| 方向 | 内容 | 优先级 |
|------|------|--------|
| RAG 知识库 | 文档入库、切片、向量检索、召回后交给 LLM 总结 | P0 |
| 商品 Agent | 商品检索、推荐理由、价格/库存由后端 Service 锁定 | P0 |
| 星座 Agent | 星座资料、运势规则、知识库检索、LLM 输出解释 | P1 |
| Agent 路由 | 用户意图识别后分发到商品、星座、知识库等服务 | P1 |
| LangGraph 编排 | 多步骤任务、工具调用、状态流转 | P2 |

核心原则：

1. LLM 负责理解和表达，不直接决定价格、权限、订单、库存。
2. Agent 负责路由和编排，不直接绕过业务 Service 查库。
3. 业务 Service 仍是唯一可信数据源。
4. AI 的价值是把已有业务能力智能化组合后输出给用户。

---

## 优先级建议

近期建议按这个顺序继续：

1. 补齐测试：先覆盖用户登录、AI 流式输出、文件上传这三条主链路。
2. 补 RBAC：用户、管理员、普通接口权限边界要先稳定。
3. 补 Redis 限流：登录、验证码、AI 调用都需要基础频控。
4. 补 AI 配置化：模型、system、搜索开关不要硬编码在业务里。
5. 补 RAG/Agent 业务原型：先做一个星座或商品场景闭环。

---

## 状态图例

- 已完成：当前代码已具备该能力。
- 待补：代码里还没有完整落地，或路由/服务未形成闭环。
- 规划中：设计方向明确，但还未开始实现。
- P0：近期必须做。
- P1：重要但可排在 P0 之后。
- P2：增强项，等主链路稳定后再做。
